import ReactDOM from 'react-dom/client'
import './index.css'
import App from './App'

// Entry point for React application, initializes and renders the root component


ReactDOM.createRoot(document.getElementById('root')).render(<App />)